#include <stdio.h>
/* include stdio.hto include the standard input-output library in c */
/*main the function that runs without declaring variables*/
/*return (0) is to indicate that our programs run*/
int main(void)
{
   puts("\"Programming is like building a multilingual puzzle");
   return (0);
}
