#include <stdio.h>
/* include stdio.h is to used to include the standard input-output library in c */
/*int main(void) is the function of our programm that runs without declaring variables*/
/*return (0) is to indicate that our programs run succesfuly wihout error*/ 
int main(void)
{
    puts("\"Programming is like building a multilingual puzzle");
    return (0);
}
