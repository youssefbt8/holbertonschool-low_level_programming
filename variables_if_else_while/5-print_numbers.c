#include <stdio.h>

/**
 * main - prints the alphabet in lowercase except q and e using while loop
 *
 * Return: 0
 */
int main(void)
{
	char number = 0;
	while (number < 10)
	{
	putchar(number);
	number++;
	}
	putchar('\n');
	return (0);
}
