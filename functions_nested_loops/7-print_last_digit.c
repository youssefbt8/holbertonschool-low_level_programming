#include "main.h"

/**
 * print_last_digit - check the code that is in the function
 * @n:the number given
 *
 * Return: Always 0.
 */
int print_last_digit(int ld)
{
	int n
	ld = n % 10
	if (ld <= 0)
	{
	ld = -ld
	else
	ld = ld
	_putchar('0' + ld)
	}
	return (0)
}
